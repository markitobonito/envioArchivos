# Example environment file for QUIC File Transfer
# Copy this to templates/quic-file-transfer/.env and fill in your values
# NEVER commit .env with real credentials to Git!

# Tailscale authentication
# Get these from https://login.tailscale.com/admin/settings/keys
TAILSCALE_AUTHKEY=tskey-auth-your-key-here
TAILSCALE_API_KEY=tskey-api-your-key-here
TAILNET=your-email@tailscale.com

# Flask environment (development or production)
FLASK_ENV=production

# Optional: Path where the container will write tailscale status
# Default: /app/tailscale_status.json
#TAILSCALE_STATUS_PATH=/app/tailscale_status.json
